export enum JsonSchemaTypes {
    OBJECT = 'object',
    STRING = 'string',
    NUMBER = 'number',
    ARRAY = 'array',
    BOOLEAN = 'boolean'
}

export enum JsonSchemaFormat {
    EMAIL = 'email',
    DATE = 'date',
    DATE_TIME = 'date-time',
    TIME = 'time'
}